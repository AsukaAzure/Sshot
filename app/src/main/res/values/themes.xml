<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme -->
    <style name="Theme.ssJanitor" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Splash screen (Android 12+): icon only, background uses wallpaper color -->
        <item name="android:windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="android:windowSplashScreenAnimationDuration">400</item>
    </style>
</resources>